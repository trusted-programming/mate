// run-rustfix
#[allow(unused_imports)]
use rayon::prelude::*;

fn main() {
    // warn_filter_simple();
}

// fn warn_filter_simple() {
//     let items = vec!["apple", "banana", "cherry"];

//     items.iter().for_each(|&item| {
//         if item.starts_with('a') {
//             println!("Starts with 'a': {}", item);
//         }
//     });
// }
