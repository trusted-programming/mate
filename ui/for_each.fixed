// run-rustfix

use rayon::prelude::*;
use std::thread;
use std::time::Duration;

fn main() {}
fn test_for_each() {
    (1..=100).into_iter().for_each(|i| {
        thread::sleep(Duration::from_secs(1));
    });
}
